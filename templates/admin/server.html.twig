{% extends 'base.html.twig' %}
{% block title %}RLM | Server Launch{% endblock %}
{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-light">
            <li class="breadcrumb-item">
                <a href="{{ path('user_home') }}">
                    Home
                </a>
            </li>
            <li class="breadcrumb-item">
                <a href="{{ path('admin_home') }}">
                    Admin
                </a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">
                Create Championship
            </li>
        </ol>
    </nav>
    <div class="row mt-5">
        <div class="col col-md-6">
            <p>
                {{ message }}
            </p>
        </div>
        <div class="col col-md-6">
            <a class="btn btn-info" href="{{ path('resultprocessing_insert', {'id': race.id}) }}">
                Get Race Results
            </a>
        </div>
    </div>
{% endblock %}