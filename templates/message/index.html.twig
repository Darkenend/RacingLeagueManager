{% extends 'base.html.twig' %}

{% block title %}RLM | Message Board{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-light">
            <li class="breadcrumb-item"><a href="{{ path('user_home') }}">Home</a></li>
            <li class="breadcrumb-item active" aria-current="page">Message Board | {{ app.user.name }} {{ app.user.lastname }}</li>
        </ol>
    </nav>
    {% if conversations|length != 0 %}
        <ul class="list-group">
            {% for conversation in conversations %}
                <li class="list-group-item">
                    <a href="{{ path('message_conversation', {'id': conversation.id}) }}">
                        {{ conversation.topic }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
